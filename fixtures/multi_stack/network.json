{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Description": "Multi-stack directory-scan fixture: network stack with an overly open security group.",
  "Resources": {
    "OpenSshSecurityGroup": {
      "Type": "AWS::EC2::SecurityGroup",
      "Properties": {
        "GroupDescription": "Intentionally open for cfn-risk-mapper directory-scan testing",
        "SecurityGroupIngress": [
          {
            "IpProtocol": "tcp",
            "FromPort": 22,
            "ToPort": 22,
            "CidrIp": "0.0.0.0/0"
          }
        ]
      }
    }
  }
}
